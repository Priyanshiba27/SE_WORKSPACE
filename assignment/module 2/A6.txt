1. Write a program that prints the numbers from 10 down to 1 using a for loop, similar to a countdown timer.
#include<stdio.h>
#include<conio.h>
int main()
{
    
    for(int i=10;i>=1;i--)
    {
        printf("\n %d",i);
    }
   
   
    
    return 0;
}

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

2. Create a menu-driven console app that lets the user: 1) View your favorite 3 IPL teams, 2) Add a new team, 3) Exit. Use a while loop to keep showing the menu until the user chooses Exit.

#include <stdio.h>
#include <conio.h>
int main()
{
    int choice=0;
    char team[50];
    
    while(choice!=3)
    {
        printf("\n To show the Above Menu Driven");
        printf("\n Press 1 for View your favorite 3 IPL teams");
        printf("\n press 2 for Add a new team");
        printf("\n press 3 for Exit");
        
        printf("\n Enter a number that you want to do : ");
        scanf("%d",&choice);
        
        if(choice==1)
        {
            printf("\n favorite Ipl team");
            printf("\n 1 RR");
            printf("\n 2 CSK");
            printf("\n 3  KKR");
        }
        if(choice==2)
        {
            printf("\n Enter Your New IPL team");
            scanf("%s",&team);
            printf("\n Congrulations to add your new ipl team %s",team);
        }
        if(choice==3)
        {
            break;
        }
    
    }
    
   return 0;
}

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

3.Build a 'Guess the Song' game like Spotify — the program randomly picks a song name from a list and asks the user to guess it. Use a do-while loop so the user can keep guessing until they get it right.

#include <stdio.h>
#include <conio.h>
void main()
{
    int select;
    int guess=4;
    do
    {
        printf("\n Guess the song");
        printf("\n 1 for Saibo");
        printf("\n 2 for One of your girls");
        printf("\n 3 for wildest dream");
        printf("\n 4 for Young and beautiful");
        printf("\n 5 for White mustang");
        printf("\n select a song : ");
        scanf("%d",&select);
        
        if(select==guess)
        {
            printf("Correct");
        }
        else
        {
            printf("Wrong");
        }
    }
    while(select!=guess);
}

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

4. Explain with your own example the difference between entry-controlled and exit-controlled loops by writing a short code snippet for each (for/while vs do-while) and describing what happens if the loop condition is false at the start.

   1) An entry-controlled loop checks the condition before executing the loop body. The for loop and while loop are entry-controlled loops. If the condition is false at the beginning, the loop body is not executed even once.

Example (while loop):
#include <stdio.h>

int main()
{
    int i = 10;

    while (i <= 5)
    {
        printf("%d ", i);
        i++;
    }

    printf("Loop Finished");
    return 0;
}

Explanation: Since the condition i <= 5 is false at the start, the while loop does not execute even once.

2) An exit-controlled loop checks the condition after executing the loop body. The do-while loop is an exit-controlled loop. Therefore, the loop body is executed at least once, even if the condition is false at the beginning.

Example (do-while loop):

#include <stdio.h>

int main()
{
    int i = 10;

    do
    {
        printf("%d ", i);
        i++;
    }
    while (i <= 5);

    return 0;
}

Explanation: The do-while loop prints 10 once because it executes the loop body before checking the condition. After that, the condition becomes false, so the loop stops.


-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

